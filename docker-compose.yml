services:
  web:
    build: .
    image: contalibra-web:latest
    container_name: contalibra-web
    restart: unless-stopped
    environment:
      - AUTH_UPSTREAM=contalibra-web-auth
    ports:
      - "8069:80"
    networks:
      - stack-net
    depends_on:
      - auth

  auth:
    build:
      context: ./auth
      ssh:
        - default=${LIBRA_WEB_KIT_SSH_KEY:-~/.ssh/id_ed25519_libra_web_kit}
    image: contalibra-web-auth:latest
    container_name: contalibra-web-auth
    restart: unless-stopped
    environment:
      - DOCS_AUTH_SECRET=${DOCS_AUTH_SECRET:-}
      - SECRET_KEY=${DOCS_SESSION_SECRET:-contalibra-docs-secret-change-me}
      - APEX_DOMAIN=${APEX_DOMAIN:-contalibra.com.ar}
    networks:
      - stack-net

networks:
  stack-net:
    external: true
    name: stack_stack-net
